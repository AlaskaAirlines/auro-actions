{
  "name": "@aurodesignsystems/auro-actions",
  "version": "0.0.0",
  "description": "Centralized repository for reusable GitHub Actions workflows and composite actions used across all Auro Design System repositories.",
  "scripts": {
    "prepare": "husky",
    "validate": "find . -name \"*.yml\" -o -name \"*.yaml\" | xargs -I {} npx action-validator {}"
  },
  "devDependencies": {
    "@action-validator/cli": "^0.6.0",
    "@action-validator/core": "^0.6.0",
    "husky": "^9.1.7"
  }
}
