name: 'build'
description: 'Builds the project'
inputs:
  os:
    description: 'Operating system to run on'
    required: true
  node-version:
    description: 'Node.js version to use'
    required: true
  cache-dirs:
    description: 'Directories to cache build artifacts'
    required: true
  additional-build-command:
    description: 'Optional command to run after build and before caching (e.g. `npx playwright install chromium`)'
    required: false
    default: ''
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
      if: ${{ github.action_repository != github.repository }}
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
    - name: NPM install
      run: npm ci
      shell: bash
    - name: Build
      run: npm run build
      shell: bash
    - name: Run additional build command
      if: ${{ inputs.additional-build-command != '' }}
      run: ${{ inputs.additional-build-command }}
      shell: bash
    - name: Save dist/demo directories to cache
      uses: actions/cache@v4
      id: build_cache
      with:
        path: ${{ inputs.cache-dirs }}
        key: build-${{ inputs.node-version }}-${{ github.run_id }}